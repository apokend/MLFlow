name: mlflow-test

conda_env: conda.yaml

entry_points:
    main:
      parameters:
        experiment: {type: string}
        uri: {type: string, default: "127.0.0.1"}
      command: "python main.py --experiment_name {experiment} --tracking_uri {uri}"

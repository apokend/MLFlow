name: mlflow-test

conda_env: conda.yaml

entry_points:
    main:
      parameters:
        experiment_name: {type: str}
        tracking_uri: {type: str, default:'http://127.0.0.1:5000'}
      command: 'python main.py --experiment_name={experiment_name} --tracking_uri={tracking_uri}'
